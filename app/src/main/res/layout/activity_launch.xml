<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".LaunchActivity">
    <com.flaviofaria.kenburnsview.KenBurnsView
        android:id="@+id/iv_kenBurnsView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <ImageView
        android:id="@+id/iv_cat"
        android:layout_width="100dp"
        android:layout_height="100dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:background="@drawable/onlylogo"/>

    <TextView
        android:id="@+id/tv_appName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        app:layout_constraintTop_toBottomOf="@id/iv_cat"
        app:layout_constraintLeft_toLeftOf="@id/iv_cat"
        app:layout_constraintRight_toRightOf="@id/iv_cat"
        android:layout_marginTop="10dp"
        android:textSize="18sp"
        android:alpha="0"
        android:textColor="@color/white"/>

</android.support.constraint.ConstraintLayout>